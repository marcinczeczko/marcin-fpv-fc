---
phase: 03-template-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - polski_fc.kicad_pro
  - kibot_yaml/kibot_pre_set_text_variables.yaml
  - kibot_yaml/kibot_main.yaml
autonomous: true
requirements: [DOCS-01]

must_haves:
  truths:
    - "Schematic PDF title block shows the correct template design"
    - "Schematic PDF displays the injected STATUS (e.g., DRAFT, RELEASED)"
    - "Schematic PDF displays the injected REVISION and TITLE"
  artifacts:
    - path: "polski_fc.kicad_pro"
      provides: "Updated worksheet paths"
    - path: "kibot_yaml/kibot_pre_set_text_variables.yaml"
      provides: "Project-wide metadata variables"
    - path: "kibot_yaml/kibot_main.yaml"
      provides: "KiBot variable injection logic"
  key_links:
    - from: "kibot_yaml/kibot_main.yaml"
      to: "kibot_yaml/kibot_pre_set_text_variables.yaml"
      via: "import directive"
---

<objective>
Update the KiCad project to use the new KDT drawing sheet templates and configure KiBot to inject project variables (STATUS, TITLE, REVISION) into the generated documentation.

Purpose: Ensure consistent branding and metadata across all generated outputs.
Output: Updated project configuration and dynamic variable injection.
</objective>

<execution_context>
@/Users/marcinczeczko/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@polski_fc.kicad_pro
@kibot_yaml/kibot_main.yaml
@kibot_resources/wks/KDT_Template.kicad_wks
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Worksheet Paths in Project</name>
  <files>polski_fc.kicad_pro</files>
  <action>
    Update the worksheet paths in `polski_fc.kicad_pro` to point to the new location in `kibot_resources/wks/`.
    - Use `sed` to replace `"Templates/KDT_Template_Fab.kicad_wks"` with `"kibot_resources/wks/KDT_Template_Fab.kicad_wks"`.
    - Use `sed` to replace `"Templates\\KDT_Template.kicad_wks"` with `"kibot_resources/wks/KDT_Template.kicad_wks"`.
    - Verify that both `pcbnew` and `schematic` sections in the JSON are updated.
  </action>
  <verify>
    <automated>grep -c "kibot_resources/wks/" polski_fc.kicad_pro</automated>
  </verify>
  <done>Worksheet paths updated to the new hierarchical structure.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Global Variables and Injection</name>
  <files>kibot_yaml/kibot_pre_set_text_variables.yaml, kibot_yaml/kibot_main.yaml</files>
  <action>
    Standardize variable injection across the configuration.
    - Ensure `kibot_yaml/kibot_pre_set_text_variables.yaml` contains:
      ```yaml
      variables:
        PROJECT_NAME: "Polski FC"
        TITLE: "Flight Controller"
        REVISION: "v1.0"
        COMPANY: "Polski FC"
      ```
    - Update `kibot_yaml/kibot_main.yaml` to ensure the `set_text_variables` pre-flight handles the `STATUS` variable based on the variant:
      ```yaml
      preflight:
        set_text_variables:
          - name: 'STATUS'
            value: '${VARIANT|DRAFT}'
          - name: 'TITLE'
            value: 'Flight Controller'
      ```
  </action>
  <verify>
    <automated>kibot -c polski_fc.kibot.yaml --print-variables</automated>
  </verify>
  <done>KiBot is configured to inject project variables and variant status.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Output PDFs</name>
  <what-built>Generated schematic PDF with template and variables.</what-built>
  <how-to-verify>
    1. Run `./kibot.sh -g DRAFT`
    2. Open `Schematic/polski_fc-schematic.pdf`.
    3. Verify the title block uses the new KDT layout.
    4. Verify that "STATUS" is displayed as "DRAFT".
    5. Run `./kibot.sh -g RELEASED` (if available) or verify other variants.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Check that all generated files in `Schematic/` reflect the correct metadata and template.
</verification>

<success_criteria>
- `polski_fc.kicad_pro` points to `kibot_resources/wks/` templates.
- KiBot correctly injects `STATUS`, `TITLE`, and `REVISION`.
- Generated schematic PDF displays the expected values in the title block.
</success_criteria>

<output>
After completion, create `.planning/phases/03-template-integration/03-01-SUMMARY.md`
</output>
