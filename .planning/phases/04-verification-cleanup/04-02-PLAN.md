---
phase: 04-verification-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [.kibot/, output/, kibot.sh, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md]
autonomous: true
requirements: [ENV-01, ENV-02]
must_haves:
  truths:
    - "Legacy .kibot/ folder is removed"
    - "Legacy output/ folder is removed"
    - "Redundant dummy files are removed"
    - "kibot.sh script is confirmed working with new structure"
    - "Project documentation reflects 100% completion of the migration"
  artifacts:
    - path: "kibot.sh"
      provides: "Main entry point script"
    - path: ".planning/STATE.md"
      provides: "Final project status"
---

<objective>
Cleanup legacy configuration files and directories, finalize the execution script, and update project documentation to mark the migration as complete.

Purpose: Maintenance and finalization of the codebase.
Output: Clean repository and updated documentation.
</objective>

<execution_context>
@/Users/marcinczeczko/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/marcinczeczko/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@kibot.sh
@.planning/phases/04-verification-cleanup/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Legacy Cleanup</name>
  <files>.kibot/, output/, dummy.kibot.yaml</files>
  <action>
    Remove legacy and redundant files:
    1. Delete the `.kibot/` directory (contains old modular configs).
    2. Delete the `output/` directory (old monolithic output path).
    3. Search for and remove any other redundant files like `dummy.kibot.yaml` or obsolete script backups.
  </action>
  <verify>
    <automated>ls -d .kibot/ output/ 2>/dev/null || echo "Cleaned"</automated>
  </verify>
  <done>Legacy files and directories removed.</done>
</task>

<task type="auto">
  <name>Task 2: Project Finalization & Documentation</name>
  <files>kibot.sh, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    Finalize the project state:
    1. Ensure `kibot.sh` has correct permissions (chmod +x) and references `polski_fc.kibot.yaml`.
    2. Update `.planning/ROADMAP.md`: Mark Phase 4 as completed.
    3. Update `.planning/STATE.md`: Update status to 100% and current phase to "Migration Completed".
    4. Update `.planning/PROJECT.md`: (Optional) Update description to reflect the new structure.
    5. Update `.planning/REQUIREMENTS.md`: Mark all v1 requirements as completed in the traceability table.
  </action>
  <verify>
    <automated>grep "Progress: 100%" .planning/STATE.md</automated>
  </verify>
  <done>Project documentation updated to reflect full completion.</done>
</task>

</tasks>

<verification>
- Verify `.kibot/` and `output/` are gone.
- Run `./kibot.sh -g DRAFT` one last time to ensure it still works.
- Verify Roadmap and State files.
</verification>

<success_criteria>
- Repository contains only the new hierarchical KiBot structure.
- `kibot.sh` successfully triggers generation using the new config.
- Documentation confirms project completion.
</success_criteria>

<output>
After completion, create `.planning/phases/04-verification-cleanup/04-02-SUMMARY.md`
</output>
