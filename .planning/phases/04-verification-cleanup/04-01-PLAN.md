---
phase: 04-verification-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Schematic/, Manufacturing/, Testing/, 3D/]
autonomous: true
requirements: [FAB-01, FAB-02, FAB-03, ASSY-01, ASSY-02, ASSY-03, DOCS-01, DOCS-02, DOCS-03]
must_haves:
  truths:
    - "KiBot generates outputs for all 4 variants (DRAFT, PRELIMINARY, CHECKED, RELEASED)"
    - "Outputs are correctly placed in hierarchical folders (Schematic, Manufacturing, 3D)"
    - "Variable injection (STATUS) matches the variant name in generated PDFs"
    - "JLCPCB ZIP contains Gerbers with correct extensions and drill files"
    - "BOM and CPL CSV files follow JLCPCB column and header requirements"
  artifacts:
    - path: "Schematic/polski_fc-schematic.pdf"
      provides: "Documentation output"
    - path: "Manufacturing/JLCPCB/polski_fc-gerbers.zip"
      provides: "Fabrication package"
    - path: "Manufacturing/JLCPCB/polski_fc-bom.csv"
      provides: "BOM for assembly"
    - path: "Manufacturing/JLCPCB/polski_fc-cpl.csv"
      provides: "Component placement list"
  key_links:
    - from: "polski_fc.kibot.yaml"
      to: "kibot_yaml/kibot_main.yaml"
      via: "import"
    - from: "kibot_yaml/kibot_main.yaml"
      to: "Schematic/"
      via: "dir setting in docs module"
---

<objective>
Exhaustively verify that the new hierarchical KiBot configuration correctly generates all required outputs for every defined variant, and validate that the manufacturing files meet JLCPCB specifications.

Purpose: Ensure the migration is functionally complete and production-ready.
Output: Validated output directories and confirmed compliance with manufacturer requirements.
</objective>

<execution_context>
@/Users/marcinczeczko/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/marcinczeczko/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@polski_fc.kibot.yaml
@kibot_yaml/kibot_main.yaml
@kibot.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Multi-Variant Execution & Directory Audit</name>
  <files>Schematic/, Manufacturing/, 3D/, Testing/</files>
  <action>
    Execute KiBot for each of the four variants using the `kibot.sh` script:
    1. Run DRAFT: `./kibot.sh -g DRAFT` -> Verify only schematic PDF is generated in `Schematic/`. Check PDF for 'DRAFT' status stamp.
    2. Run PRELIMINARY: `./kibot.sh -g PRELIMINARY` -> Verify schematic PDF and iBOM.
    3. Run CHECKED: `./kibot.sh -g CHECKED` -> Verify all outputs (Gerbers, BOM, CPL, 3D).
    4. Run RELEASED: `./kibot.sh -g RELEASED` -> Verify all outputs. Check PDF for 'RELEASED' status stamp.
    
    Ensure NO files are generated in the root or in the legacy `output/` directory.
  </action>
  <verify>
    <automated>ls -R Schematic/ Manufacturing/ 3D/ Testing/</automated>
  </verify>
  <done>All variants produce expected files in correct directories; STATUS variable injection confirmed in PDFs.</done>
</task>

<task type="auto">
  <name>Task 2: JLCPCB Compliance Deep-Dive</name>
  <files>Manufacturing/JLCPCB/</files>
  <action>
    Inspect the generated JLCPCB package:
    1. Unzip `Manufacturing/JLCPCB/polski_fc-gerbers.zip` and verify Gerber extensions (e.g., .GTL, .GBL, .GTS, .GBS, .GTO, .GBO, .GML/GKO).
    2. Verify drill files (.drl) are present and merged if configured.
    3. Open `polski_fc-bom.csv` and ensure headers match JLCPCB requirements (Designator, Footprint, LCSC Part #, Value, etc.).
    4. Open `polski_fc-cpl.csv` and ensure headers match (Designator, Mid X, Mid Y, Layer, Rotation).
    5. Check that rotation offsets (if any) are being applied (this would require looking at the CSV content vs KiCad PCB, but a sanity check of the file structure is primary).
  </action>
  <verify>
    <automated>head -n 5 Manufacturing/JLCPCB/polski_fc-bom.csv Manufacturing/JLCPCB/polski_fc-cpl.csv</automated>
  </verify>
  <done>Manufacturing outputs confirmed compliant with JLCPCB standards.</done>
</task>

</tasks>

<verification>
- Run all 4 variants via `kibot.sh`.
- Check `Schematic/` for correct PDFs.
- Check `Manufacturing/JLCPCB/` for ZIP and CSVs.
- Check `3D/` for STEP.
</verification>

<success_criteria>
- Zero outputs in legacy `output/` or root directory.
- All 4 variants successfully generate their respective output groups.
- JLCPCB ZIP contains valid Gerbers and drill files.
- BOM and CPL headers are correct.
</success_criteria>

<output>
After completion, create `.planning/phases/04-verification-cleanup/04-01-SUMMARY.md`
</output>
