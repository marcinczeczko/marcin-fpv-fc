---
phase: 02-configuration-refactoring
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified: [kibot_yaml/kibot_main.yaml, polski_fc.kibot.yaml]
autonomous: true
requirements: [MODULARITY, CONFIG_MAIN, VARIANT_LOGIC, METADATA]

must_haves:
  truths:
    - "kibot_yaml/kibot_main.yaml exists and imports all modules"
    - "polski_fc.kibot.yaml imports kibot_yaml/kibot_main.yaml"
    - "kibot -v DRAFT correctly only runs schematic_pdf (verified by dry run)"
  artifacts:
    - path: "kibot_yaml/kibot_main.yaml"
      provides: "Central configuration and variant logic"
    - path: "polski_fc.kibot.yaml"
      provides: "Main project entry point"
  key_links:
    - from: "polski_fc.kibot.yaml"
      to: "kibot_yaml/kibot_main.yaml"
      via: "import: kibot_yaml/kibot_main.yaml"
---

<objective>
Implement the main configuration entry point and the variant logic to control project status and output generation.

Purpose: Orchestrate the modular configuration and provide a high-level interface for different project states (DRAFT, RELEASED, etc.).
Output: kibot_yaml/kibot_main.yaml and updated polski_fc.kibot.yaml.
</objective>

<execution_context>
@/Users/marcinczeczko/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-configuration-refactoring/02-01-RESEARCH.md
@kibot_yaml/kibot_pre_set_text_variables.yaml
@kibot_yaml/kibot_pre_flights.yaml
@kibot_yaml/kibot_out_docs.yaml
@kibot_yaml/kibot_out_fab.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Main Orchestrator</name>
  <files>kibot_yaml/kibot_main.yaml</files>
  <action>
    Create kibot_yaml/kibot_main.yaml with:
    - 'global: out_dir: "."'
    - 'import' list:
      - kibot_pre_set_text_variables.yaml
      - kibot_pre_flights.yaml
      - kibot_out_docs.yaml
      - kibot_out_fab.yaml
    - 'preflight: set_text_variables' mapping 'STATUS' to '${VARIANT|DRAFT}'
    - 'variants' section:
      - 'DRAFT': (No additional outputs, only schematic runs by default)
      - 'PRELIMINARY': Enable 'ibom'
      - 'CHECKED': Enable 'ibom', 'step_3d', and all FAB/ASSEMBLY outputs.
      - 'RELEASED': Same as CHECKED.
  </action>
  <verify>
    <automated>ls kibot_yaml/kibot_main.yaml && grep "out_dir: \"\.\"" kibot_yaml/kibot_main.yaml</automated>
  </verify>
  <done>Main orchestrator created with variant logic and module imports.</done>
</task>

<task type="auto">
  <name>Task 2: Update Project Entry Point</name>
  <files>polski_fc.kibot.yaml</files>
  <action>
    Modify polski_fc.kibot.yaml in the project root to:
    - Keep 'kibot' version header.
    - Remove old 'global' and 'import' sections.
    - Add 'import: - kibot_yaml/kibot_main.yaml'.
  </action>
  <verify>
    <automated>grep "kibot_yaml/kibot_main.yaml" polski_fc.kibot.yaml</automated>
  </verify>
  <done>Root configuration updated to point to the new hierarchical structure.</done>
</task>

<task type="auto">
  <name>Task 3: Final Verification</name>
  <files>polski_fc.kibot.yaml</files>
  <action>
    Run KiBot dry runs for different variants to verify the configuration is valid and outputs are correctly filtered.
    - Run 'kibot -n -v DRAFT'
    - Run 'kibot -n -v RELEASED'
    Verify that the 'out_dir' and 'dir' logic results in the expected paths.
  </action>
  <verify>
    <automated>./kibot.sh -n -v DRAFT && ./kibot.sh -n -v RELEASED</automated>
  </verify>
  <done>Refactored configuration verified for syntax and variant logic.</done>
</task>

</tasks>

<verification>
Manual verification of the output paths in the dry run logs:
./kibot.sh -n -v RELEASED | grep "Directory:"
</verification>

<success_criteria>
- kibot_yaml/kibot_main.yaml correctly orchestrates all modules.
- polski_fc.kibot.yaml is a clean entry point.
- KiBot runs successfully in both DRAFT and RELEASED variants.
- Outputs are correctly assigned to project-level directories.
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-refactoring/02-01-03-SUMMARY.md`
</output>
