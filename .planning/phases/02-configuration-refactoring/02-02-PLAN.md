---
phase: 02-configuration-refactoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [kibot_yaml/kibot_out_fab.yaml]
autonomous: true
requirements: [MODULARITY, REFACTOR_FAB]

must_haves:
  truths:
    - "kibot_yaml/kibot_out_fab.yaml exists and contains all fabrication outputs (Gerbers, Drill, BOM, Position)"
    - "Fabrication outputs are redirected to Manufacturing/"
  artifacts:
    - path: "kibot_yaml/kibot_out_fab.yaml"
      provides: "Fabrication and assembly outputs"
  key_links:
    - from: "kibot_yaml/kibot_out_fab.yaml"
      to: "Manufacturing/"
      via: "dir: Manufacturing"
---

<objective>
Refactor fabrication and assembly KiBot configurations into the new modular structure with redirection to the Manufacturing directory.

Purpose: Consolidate Gerber, Drill, BOM, and Position outputs into a single module and redirect them to Manufacturing/.
Output: A new KiBot module: kibot_yaml/kibot_out_fab.yaml.
</objective>

<execution_context>
@/Users/marcinczeczko/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.kibot/fab.kibot.yaml
@.kibot/jlcpcb.kibot.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Fabrication Module</name>
  <files>kibot_yaml/kibot_out_fab.yaml</files>
  <action>
    Port 'outputs' from .kibot/fab.kibot.yaml and .kibot/jlcpcb.kibot.yaml into kibot_yaml/kibot_out_fab.yaml.
    Update 'dir' settings for ALL outputs to 'Manufacturing' (or subdirectories like 'Manufacturing/Gerber', 'Manufacturing/Drill').
    IMPORTANT: Ensure the 'JLCPCB_zip' output correctly references the renamed/moved outputs if necessary.
    Add 'run_by_default: false' to ALL outputs in this file (they will be enabled by 'CHECKED' and 'RELEASED' variants).
    Use the '_JLCPCB' templates where applicable or maintain the existing settings.
  </action>
  <verify>
    <automated>ls kibot_yaml/kibot_out_fab.yaml && grep "dir: Manufacturing" kibot_yaml/kibot_out_fab.yaml</automated>
  </verify>
  <done>Fabrication module created with redirected paths and 'run_by_default: false' flags.</done>
</task>

</tasks>

<verification>
Check syntax of the new file using KiBot:
kibot -n -c kibot_yaml/kibot_out_fab.yaml
</verification>

<success_criteria>
- kibot_yaml/kibot_out_fab.yaml exists and is syntactically correct.
- All outputs in the file are directed to the Manufacturing/ directory.
- All outputs have run_by_default: false.
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-refactoring/02-01-02-SUMMARY.md`
</output>
