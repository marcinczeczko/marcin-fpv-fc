---
phase: 02-configuration-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [kibot_yaml/kibot_pre_flights.yaml, kibot_yaml/kibot_out_docs.yaml]
autonomous: true
requirements: [MODULARITY, REFACTOR_DOCS, REFACTOR_BASE]

must_haves:
  truths:
    - "kibot_yaml/kibot_pre_flights.yaml exists and contains filters from base.kibot.yaml"
    - "kibot_yaml/kibot_out_docs.yaml exists and uses redirected directories (Schematic, 3D)"
  artifacts:
    - path: "kibot_yaml/kibot_pre_flights.yaml"
      provides: "Pre-flight filters and test settings"
    - path: "kibot_yaml/kibot_out_docs.yaml"
      provides: "Documentation and 3D outputs"
  key_links:
    - from: "kibot_yaml/kibot_out_docs.yaml"
      to: "Schematic/"
      via: "dir: Schematic"
    - from: "kibot_yaml/kibot_out_docs.yaml"
      to: "3D/"
      via: "dir: 3D"
---

<objective>
Refactor base and documentation KiBot configurations into the new modular structure with output redirection.

Purpose: Move configuration to the kibot_yaml/ directory and ensure documentation outputs go to project-level folders.
Output: Two new KiBot modules: kibot_pre_flights.yaml and kibot_out_docs.yaml.
</objective>

<execution_context>
@/Users/marcinczeczko/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-configuration-refactoring/02-01-RESEARCH.md
@.kibot/base.kibot.yaml
@.kibot/docs.kibot.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pre-flight Module</name>
  <files>kibot_yaml/kibot_pre_flights.yaml</files>
  <action>
    Port the 'preflight' section from .kibot/base.kibot.yaml into a new file kibot_yaml/kibot_pre_flights.yaml.
    Ensure 'erc', 'drc', 'check_zone_fills' are set to true.
    Include all filters defined in the original file.
    Keep the 'kibot' version header.
  </action>
  <verify>
    <automated>ls kibot_yaml/kibot_pre_flights.yaml && grep "filters:" kibot_yaml/kibot_pre_flights.yaml</automated>
  </verify>
  <done>Pre-flight module created with all legacy filters.</done>
</task>

<task type="auto">
  <name>Task 2: Create Documentation Module</name>
  <files>kibot_yaml/kibot_out_docs.yaml</files>
  <action>
    Port the 'outputs' from .kibot/docs.kibot.yaml into kibot_yaml/kibot_out_docs.yaml.
    Update 'dir' settings:
    - 'schematic_pdf' -> 'Schematic'
    - 'ibom' -> 'Schematic' (or 'Testing' if appropriate, but Schematic is safer for docs)
    - 'step_3d' -> '3D'
    Add 'run_by_default: false' to 'ibom' and 'step_3d' (will be enabled by variants later).
    Keep 'schematic_pdf' as 'run_by_default: true' (default for DRAFT).
  </action>
  <verify>
    <automated>ls kibot_yaml/kibot_out_docs.yaml && grep "dir: Schematic" kibot_yaml/kibot_out_docs.yaml</automated>
  </verify>
  <done>Documentation module created with redirected paths and default run flags.</done>
</task>

</tasks>

<verification>
Check syntax of the new files using KiBot:
kibot -n -c kibot_yaml/kibot_pre_flights.yaml
kibot -n -c kibot_yaml/kibot_out_docs.yaml
</verification>

<success_criteria>
- kibot_yaml/kibot_pre_flights.yaml exists and is syntactically correct.
- kibot_yaml/kibot_out_docs.yaml exists and points to Schematic/ and 3D/.
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-refactoring/02-01-01-SUMMARY.md`
</output>
