name: Hardware CI/CD (KiBot)

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]
  workflow_dispatch:
    inputs:
      target:
        description: 'KiBot Target Group to run'
        required: true
        default: 'DRAFT'
        type: choice
        options:
          - DRAFT
          - PRELIMINARY
          - CHECKED
          - RELEASED

jobs:
  kibot:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Determine Target
        id: tgt
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "target=${{ github.event.inputs.target }}" >> $GITHUB_OUTPUT
          elif [ "${{ github.ref }}" == "refs/heads/main" ]; then
            echo "target=RELEASED" >> $GITHUB_OUTPUT
          else
            echo "target=DRAFT" >> $GITHUB_OUTPUT
          fi

      - name: Generate Outputs (${{ steps.tgt.outputs.target }})
        uses: INTI-CMNB/KiBot@v2_k9
        with:
          config: polski_fc.kibot.yaml
          targets: ${{ steps.tgt.outputs.target }}

      - name: Commit Updated Documentation
        if: github.event_name == 'push'
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "docs(automation): update ${{ steps.tgt.outputs.target }} outputs [skip ci]"
          file_pattern: 'Schematic/* Manufacturing/* 3D/*'

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: polski_fc-${{ steps.tgt.outputs.target }}-artifacts
          path: |
            Schematic/
            Manufacturing/
            3D/
